@using Bookit.Common
@model Bookit.Models.BookViewModel

<div class="row">
    <div class="col-sm-2">
        <img src="@Model.ImagePath" alt="@Model.ImagePath" class="img-responsive" />
    </div>
    <div class="col-sm-10">
        <h4>@Model.Name</h4>

        <p>Price : @Model.Price</p>
        <p>
            <a asp-action="more" asp-route-id="@Model.Id" asp-controller="Book" asp-area="" class="btn btn-primary">More...</a>
            <a asp-action="create" asp-route-bookid="@Model.Id" asp-controller="order" asp-area="" class="btn btn-success" style="width:200px"> Order now</a>
            <a href="#" class="btn btn-primary addToBasket" data-book="@Model.ToJson()">Add</a>
        </p>
    </div>
</div>